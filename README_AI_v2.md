# 🤖 AI Portfolio Generator v2.0

**Полная система генерации уникальных портфолио на основе нейросети**

---

## 🎯 Что это делает?

Приложение анализирует **все успешные портфолио** со страницы https://portfolios.hivemindrobotics.net/ftc и использует нейросеть для генерирования новых уникальных комбинаций дизайна:

✨ **AI анализирует:**
- Цветовые схемы успешных портфолио
- Типографику (шрифты)  
- Стили элементов (карты, тени, скругления)
- Успешные паттерны дизайна

🎨 **Генерирует новые:**
- Комбинации дизайнов (микс успешных элементов)
- HTML портфолио
- PDF файлы (можно скачивать)
- Полностью оригинальные, но基于 реальных успешных примерах

---

## 📦 Структура проекта

```
portfolio-constructor/
├── backend/
│   ├── app.py                         # Основной Flask API (старая версия)
│   ├── app_v2.py                      # Новый API с AI функциями
│   ├── advanced_scraper.py            # Скрэйпер для скачивания портфолио
│   ├── portfolio_design_neural.py     # Нейросеть для анализа дизайна
│   ├── portfolio_visualizer.py        # Генератор HTML/PDF из дизайна
│   ├── ai_portfolio_generator_v2.py   # Главный скрипт полного цикла
│   ├── portfolio_generator.py         # Основной генератор
│   ├── data/
│   │   ├── ftc_portfolios.json       # База портфолио
│   │   └── portfolio_templates.json  # Шаблоны макетов
│   └── requirements.txt               # Python зависимости
│
├── frontend/
│   ├── index.html                     # Старый интерфейс
│   ├── index_v2.html                  # Новый AI интерфейс (4 вкладки)
│   ├── script.js                      # Старый JavaScript
│   └── styles.css                     # Старые стили
│
└── README.md
```

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd backend
pip install -r requirements.txt
```

**Основные пакеты:**
- `Flask` - веб-фреймворк
- `selenium` - парсинг JavaScript контента
- `beautifulsoup4` - парсинг HTML
- `reportlab` - генерация PDF
- `scikit-learn` - TF-IDF и ML
- `numpy` - математика для нейросети

### 2. Полный цикл (скачать + обучить + генерировать)

```bash
python ai_portfolio_generator_v2.py
```

Это:
1. ✅ Скачивает все портфолио со страницы (Selenium + BeautifulSoup)
2. ✅ Анализирует дизайн каждого
3. ✅ Обучает нейросеть на успешных примерах
4. ✅ Генерирует 10 новых уникальных дизайнов
5. ✅ Экспортирует в HTML + PDF
6. ✅ Создаёт галерею для просмотра

**Результат:** папка `backend/generated_portfolios/` с дизайнами

### 3. Запуск Flask API

```bash
python app_v2.py
```

API будет доступен на `http://localhost:5000`

### 4. Открыть веб-интерфейс

В браузер открыть:
```
file:///c:/Users/ilaci/portfolio%20constructor/portfolio-constructor/frontend/index_v2.html
```

---

## 🎨 API Endpoints

### AI функции

**POST** `/api/generate-ai-mix`
```json
{
  "count": 5
}
```
Генерирует N новых дизайнов как микс успешных портфолио

Response:
```json
{
  "success": true,
  "count": 5,
  "designs": [
    {
      "id": "generated_1",
      "name": "AI Mix Design #1",
      "pattern": "modern",
      "colors": {
        "primary": "#667eea",
        "secondary": "#764ba2",
        "accent": "#f093fb"
      },
      "fonts": {
        "heading": "Inter, sans-serif",
        "body": "Open Sans, sans-serif"
      },
      "pattern_confidence": 0.85,
      "success_probability": 0.78,
      "design_attributes": {...}
    }
  ]
}
```

**POST** `/api/design-to-html`
```json
{ "design": {...} }
```
Преобразует параметры дизайна в HTML

**POST** `/api/design-to-pdf`
```json
{ "design": {...} }
```
Преобразует параметры дизайна в PDF (скачивается как файл)

### Стандартные функции

**GET** `/api/health`
Проверка статуса и количество портфолио

**GET** `/api/search-ftc?q=query`
Поиск портфолио по названию

**GET** `/api/portfolios?limit=50`
Получить список портфолио

**POST** `/api/generate-portfolio`
```json
{
  "prompt": "современный минималистичный",
  "count": 3
}
```
Генерирует макеты на основе текстового промта

---

## 📊 Как работает нейросеть?

### 1. Анализ (PortfolioDesignAnalyzer)

```python
analyzer = PortfolioDesignAnalyzer(portfolios)
```

Извлекает из каждого портфолио:
- **Сложность дизайна** (0.0 - 1.0)
- **Успешность** (0.0 - 1.0)
- **Цветовая палитра** (10 популярных комбинаций)
- **Типография** (8 комбинаций шрифтов)
- **Паттерны дизайна** (6 стилей):
  - minimal (минималистичный)
  - modern (современный)
  - gradient (градиентный)
  - glassmorphism (стекло-подобный)
  - bold (смелый)
  - neumorphic (нейморфный)

### 2. Нейросеть (PortfolioDesignNeuralNet)

```python
neural_net = PortfolioDesignNeuralNet(features)
neural_net.train()  # Обучение
design = neural_net.predict(complexity, success_score, seed)
```

**Архитектура:**
- Входной слой: 3 параметра (team_id, complexity, success_score)
- Скрытый слой: 16 нейронов (ReLU активация)
- Выходной слой: 16 нейронов (softmax)
  - Первые 6: выбор паттерна дизайна
  - Следующие 10: выбор цветовой схемы

### 3. Генерация (PortfolioMixGenerator)

```python
generator = PortfolioMixGenerator(portfolios)
designs = generator.generate_mix_designs(count=5)
```

Генерирует новые комбинации:
- Усредняет параметры успешных портфолио
- Использует нейросеть для предсказания лучших комбинаций
- Возвращает JSON с полными параметрами дизайна

### 4. Визуализация (PortfolioDesignVisualizer)

```python
visualizer = PortfolioDesignVisualizer()

# HTML
html = visualizer.generate_html_portfolio(design)

# PDF
pdf_bytes = visualizer.generate_pdf_portfolio(design)
```

Генерирует:
- Полностью функциональный HTML с CSS
- PDF с информацией о дизайне и цветовой схемой

---

## 🖼️ Примеры дизайнов

Приложение генерирует дизайны в 6 стилях:

### 1. **Minimal** - Минималистичный
```
Белое пространство, 8px скругления, subtle тень
Хорош для: современных, чистых проектов
```

### 2. **Modern** - Современный
```
Outlined карты, 12px скругления, medium тень
Хорош для: профессиональных портфолио
```

### 3. **Gradient** - Градиентный
```
Градиентные переходы, 16px скругления, soft тень
Хорош для: творческих проектов
```

### 4. **Glassmorphism** - Стекло-подобный
```
Полупрозрачные элементы, backdrop blur, 20px скругления
Хорош для: современных, минималистичных дизайнов
```

### 5. **Bold** - Смелый
```
Сплошные цвета, 4px скругления, strong тень
Хорош для: ярких, энергичных проектов
```

### 6. **Neumorphic** - Нейморфный
```
Объёмные элементы, inset тени, 20px скругления
Хорош для: изящных, современных интерфейсов
```

---

## 📈 Процесс обучения нейросети

1. **Инициализация** - случайные веса
2. **Анализ успешных портфолио** - фильтруем с высоким рейтингом
3. **Обучение** - обновляем веса на основе успешных примеров (100 эпох)
4. **Предсказание** - генерируем новые комбинации через forward pass

```python
# Forward pass
hidden = ReLU(input @ W1 + b1)
output = softmax(hidden @ W2 + b2)

# Выбираем лучшие паттерны и цвета
pattern_scores = output[:6]
color_scores = output[6:]
```

---

## 🔧 Расширение функциональности

### Добавить новый стиль дизайна

В `portfolio_design_neural.py`:

```python
DESIGN_PATTERNS = {
    'new_style': {
        'description': 'Описание нового стиля',
        'card_style': 'solid',
        'border_radius': '12px',
        'shadow': 'medium',
        'spacing': 'standard'
    }
}
```

### Добавить новую цветовую схему

```python
POPULAR_COLOR_SCHEMES = [
    # ... существующие ...
    {'primary': '#FF6B6B', 'secondary': '#4ECDC4', 'accent': '#FFE66D'},
]
```

### Обучить на собственных данных

```python
# Загрузить свои портфолио
custom_portfolios = json.load(open('my_portfolios.json'))

# Создать генератор
generator = PortfolioMixGenerator(custom_portfolios)

# Генерировать дизайны
designs = generator.generate_mix_designs(count=10)
```

---

## 🐛 Troubleshooting

### Selenium не работает
```
Решение: Убедитесь что установлен ChromeDriver
pip install webdriver-manager
```

### API не запускается
```
Решение: Проверьте что porttions 5000 не занят
lsof -i :5000  # Linux/Mac
netstat -ano | findstr :5000  # Windows
```

### Нейросеть генерирует странные комбинации
```
Решение: Данных немного. Добавьте больше портфолио или увеличьте epochs в train()
```

---

## 📝 Файлы конфигурации

### requirements.txt
```
Flask==2.3.3
scikit-learn==1.3.1
selenium>=4.10.0
beautifulsoup4==4.12.2
reportlab==4.0.7
numpy>=1.24.0
```

### data/ftc_portfolios.json
```json
{
  "portfolios": [
    {
      "team_number": "10179",
      "team_name": "Tech Turtles",
      "achievement": "Winner",
      "portfolio_type": "cs",
      "pdf_url": "https://cdn.hivemindrobotics.net/portfolios/10179-cs.pdf"
    }
  ]
}
```

---

## 🎓 Как работает скрэйпер

### Selenium метод
```python
scraper = AdvancedFTCPortfolioScraper()
portfolios = scraper.scrape_with_selenium()
```

1. Открывает браузер Chrome
2. Заходит на сайт
3. Скролит до конца (загружает все элементы)
4. Парсит все ссылки на PDF
5. Извлекает информацию о команде

### Fallback метод (если Selenium не работает)
```python
portfolios = scraper.scrape_fallback()
```

Использует BeautifulSoup для парсинга статического HTML

---

## 🌐 Интеграция с фронтенд

### Вкладка "AI Mix Designer"
```
1. Вводим количество дизайнов
2. Жмём "Генерировать дизайны"
3. API: POST /api/generate-ai-mix
4. Получаем красивые карточки с дизайнами
5. Можем скачать каждый как HTML или PDF
```

### Вкладка "Search Portfolios"
```
1. Вводим поиск (например "Tech")
2. API: GET /api/search-ftc?q=Tech
3. Видим реальные портфолио FTC команд
4. Можем скачать оригинальный PDF
```

### Вкладка "Generate Layouts"
```
1. Описываем что нам нужно (промт)
2. API: POST /api/generate-portfolio
3. Получаем 3 макета с релевантностью
4. Это текстовый, а не AI метод
```

### Вкладка "Admin"
```
1. Видим статистику (кол-во портфолио, стилей)
2. Кнопка сканирования для обновления базы
3. API: POST /api/scan-portfolios
```

---

## 📊 Статистика

```
✓ Всего портфолио: 30+
✓ Дизайн паттернов: 6
✓ Цветовых схем: 10
✓ Шрифтовых комбинаций: 8
✓ Скрытых нейронов: 16
✓ Всего параметров сети: ~400
```

---

## 🎯 Следующие шаги

### Улучшения
- [ ] Добавить Selenium для полного скрэйпинга (требует ChromeDriver)
- [ ] Увеличить датасет (скачать всех портфолио со страницы)
- [ ] Глубокое обучение (CNN для анализа изображений портфолио)
- [ ] Сохранение истории генераций
- [ ] Редактор дизайна в реальном времени

### Экспоненциально (если нужно)
- [ ] TensorFlow/PyTorch для более сложных моделей
- [ ] GAN для генерации совершенно новых дизайнов
- [ ] Загрузка собственных портфолио
- [ ] Обучение на стилях конкретных команд

---

## 📞 Поддержка

Если что-то не работает:
1. Проверьте логи приложения
2. Убедитесь что все зависимости установлены
3. Перезагрузите API сервер

---

**Enjoy your AI-powered portfolio generator! 🚀✨**
