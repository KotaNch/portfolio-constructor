# 📖 Portfolio Templates Generator - Полное руководство

## 🎯 Назначение

Генератор автоматически создаёт 1000+ готовых PDF шаблонов портфолио для FTC с разными:
- Типами макетов (10 вариантов)
- Цветовыми схемами (10 вариантов)
- Описаниями и тегами
- Количеством страниц (10-15)

Каждый шаблон готов к скачиванию и заполнению контентом.

## 🚀 Быстрый старт

### Вариант 1: Python (рекомендуется)
```bash
cd portfolio_templates_generator
python quick_start.py
```

### Вариант 2: BAT файл (Windows)
```bash
double-click START.bat
```

### Вариант 3: Полный контроль
```bash
python main.py --generate 1000  # Генерировать 1000 шаблонов
python main.py --test           # Тестировать поиск
python main.py --api            # Запустить API
```

## 📊 Что генерируется

### Каждый PDF шаблон включает:

**Титульная страница**
- Названия портфолио
- Дизайн-схема (цвета)
- Тип макета

**Страница "О команде"**
- 2 колонки для опыта и достижений
- Фон и рамки нужного цвета

**Страницы проектов (галерея)**
- Сетка 2x2 для проектов
- Цветные блоки-прямоугольники

**Страницы контента**
- 3 колонки для описаний
- Структурированный макет

**Страницы достижений**
- Список наград/достижений
- Подсвеченные блоки

## 🔍 Поиск и использование

### Через Python
```python
from search_engine import TemplateSearchEngine

engine = TemplateSearchEngine()

# Поиск по промту
results = engine.search('Современный синий минималистичный', limit=5)
for template in results:
    print(f"{template['filename']}: {template['relevance_score']:.0%}")
```

### Через API (HTTP)

**Поиск по промту:**
```bash
curl -X POST http://localhost:5001/api/search \
  -H "Content-Type: application/json" \
  -d '{"prompt": "Современный дизайн", "limit": 5}'
```

**Получить случайные:**
```bash
curl http://localhost:5001/api/random?count=3
```

**Информация:**
```bash
curl http://localhost:5001/api/info
```

**Скачать:**
```bash
curl -O http://localhost:5001/api/download/template_00001
```

### Через JavaScript
```javascript
// Поиск
async function searchTemplates(prompt) {
  const res = await fetch('/api/templates/search', {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({prompt, limit: 5})
  });
  const data = await res.json();
  return data.templates;
}

// Использование
const templates = await searchTemplates('Синий минималистичный');
templates.forEach(t => {
  console.log(`${t.filename}: ${(t.relevance_score * 100).toFixed(0)}% релевантности`);
});
```

## 📁 Структура результатов

После генерирования создаётся:

```
pdf_templates/
├── template_00001.pdf          # PDF файл
├── template_00002.pdf
├── ...
├── template_01000.pdf
├── metadata/
│   ├── template_00001.json     # Метаданные
│   ├── template_00002.json
│   └── ...
└── index.json                  # Индекс всех шаблонов
```

**Пример метаданных (JSON):**
```json
{
  "id": "template_00001",
  "filename": "template_00001.pdf",
  "layout_type": "modern_minimal",
  "colors": {
    "primary": "#1E88E5",
    "secondary": "#FFF3E0",
    "accent": "#FF6F00"
  },
  "pages": 12,
  "created_at": "2026-01-14T19:30:00",
  "tags": [
    "modern_minimal",
    "синий",
    "минималистичный",
    "творческий",
    "профессиональный",
    "портфолио",
    "ftc"
  ],
  "description": "Современный минималистичный дизайн с фокусом на контент"
}
```

## 🎨 Доступные типы макетов

| Тип | Описание |
|-----|---------|
| `modern_minimal` | Современный минималистичный дизайн |
| `corporate_bold` | Корпоративный с смелыми акцентами |
| `creative_artistic` | Творческий и артистичный |
| `tech_focused` | Для IT и технологических проектов |
| `nature_inspired` | Вдохновлённый природой |
| `minimalist_clean` | Чистый и простой |
| `dark_professional` | Тёмный профессиональный стиль |
| `colorful_vibrant` | Яркий многоцветный |
| `elegant_classic` | Элегантный классический |
| `startup_modern` | Для стартапов |

## 💾 Примеры использования

### Пример 1: Найти для FTC робо-клуба

```python
results = engine.search('Портфолио для FTC команды робоклуба', limit=3)

for template in results:
    print(f"Шаблон: {template['id']}")
    print(f"Макет: {template['layout_type']}")
    print(f"Совпадение: {template['relevance_score']:.0%}\n")
```

**Результат:**
```
Шаблон: template_00001
Макет: tech_focused
Совпадение: 92%

Шаблон: template_00045
Макет: startup_modern
Совпадение: 85%

Шаблон: template_00123
Макет: modern_minimal
Совпадение: 78%
```

### Пример 2: Фильтр по тегам

```python
results = engine.get_by_tags(['синий', 'профессиональный'], limit=5)
print(f"Найдено шаблонов с синим и профессиональным стилем: {len(results)}")
```

### Пример 3: По типу макета

```python
modern = engine.get_by_layout('modern_minimal', limit=5)
print(f"Современных минималистичных шаблонов: {len(modern)}")
```

## ⚙️ Конфигурирование

Отредактируйте `config.py` для изменения:
- Количества генерируемых шаблонов
- Типов макетов
- Цветовых схем
- Параметров API
- Параметров поиска

## 🔗 Интеграция с основным приложением

### 1. Скопируйте папку генератора
```bash
cp -r portfolio_templates_generator portfolio-constructor/
```

### 2. Добавьте в `backend/app.py`
```python
from portfolio_templates_generator.search_engine import TemplateSearchEngine

templates_engine = TemplateSearchEngine(
    metadata_dir='../portfolio_templates_generator/pdf_templates/metadata',
    index_file='../portfolio_templates_generator/pdf_templates/index.json'
)

@app.route('/api/templates/search', methods=['POST'])
def search_templates():
    data = request.json
    results = templates_engine.search(data.get('prompt', ''), limit=5)
    return jsonify({'templates': results})
```

### 3. В фронтенде используйте
```javascript
const templates = await fetch('/api/templates/search', {
  method: 'POST',
  body: JSON.stringify({prompt: userInput})
}).then(r => r.json());
```

## 📊 Производительность

| Операция | Время |
|----------|-------|
| Генерирование 100 шаблонов | 2-3 мин |
| Генерирование 1000 шаблонов | 20-30 мин |
| Загрузка индекса | < 1 сек |
| Поиск по промту | < 100 мс |
| Поиск по тегам | < 50 мс |

## 🐛 FAQ

**Q: Сколько места займут 1000 шаблонов?**  
A: Примерно 500-700 МБ для PDF + 50-100 МБ метаданных

**Q: Можно ли увеличить количество?**  
A: Да, отредактируйте в `config.py` или передайте параметр:
```bash
python main.py --generate 10000
```

**Q: Можно ли изменить размер PDF?**  
A: Да, отредактируйте `GENERATION_CONFIG` в `config.py`

**Q: Как добавить свои цвета?**  
A: Добавьте в `COLOR_SCHEMES` в `config.py`

**Q: Как использовать с Docker?**  
A: Добавьте Dockerfile в папку генератора и соберите образ

## 📝 Логирование

Логи сохраняются в `logs/generator.log`

Просмотр логов:
```bash
tail -f logs/generator.log
```

## 🆘 Решение проблем

**Ошибка: reportlab не установлен**
```bash
pip install reportlab
```

**Ошибка: scikit-learn не установлен**
```bash
pip install scikit-learn
```

**API не запускается**
Проверьте что порт 5001 не занят:
```bash
netstat -ano | findstr :5001
```

Измените порт в `api.py`:
```python
app.run(port=5002)
```

---

**Версия:** 1.0.0  
**Последнее обновление:** 2026-01-14  
**Статус:** ✅ Production Ready
