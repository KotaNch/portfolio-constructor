# 📚 Portfolio Templates Generator

Система для генерирования 1000+ готовых PDF шаблонов портфолио для FTC с поисковой системой и API.

## 🎯 Возможности

✅ **Генерирование 1000+ PDF шаблонов** - каждый на 10-15 страниц  
✅ **Автоматическая каталогизация** - метаданные и теги для каждого  
✅ **Умный поиск** - поиск по промту используя TF-IDF  
✅ **REST API** - для интеграции с основным проектом  
✅ **Скачивание** - пользователи могут скачивать выбранные шаблоны  

## 📁 Структура

```
portfolio_templates_generator/
├── main.py                 # Главный скрипт
├── generate_templates.py   # Генератор PDF
├── search_engine.py        # Поисковый движок
├── api.py                  # REST API
├── requirements.txt        # Зависимости
└── pdf_templates/          # Выходная папка
    ├── template_00001.pdf
    ├── template_00002.pdf
    ├── ... (1000+ файлов)
    ├── metadata/           # Метаданные
    │   ├── template_00001.json
    │   └── ...
    └── index.json          # Индекс всех шаблонов
```

## 🚀 Быстрый старт

### 1️⃣ Установка зависимостей

```bash
python main.py --install
```

### 2️⃣ Генерирование шаблонов (100 для теста)

```bash
python main.py --generate 100
```

Для полного набора:
```bash
python main.py --generate 1000
```

⏱️ **Примерное время:** 
- 100 шаблонов: 2-3 минуты
- 1000 шаблонов: 20-30 минут

### 3️⃣ Тестирование поиска

```bash
python main.py --test
```

### 4️⃣ Запуск API

```bash
python main.py --api
```

API запустится на `http://localhost:5001`

### 🎯 Полный процесс

```bash
python main.py --full
```

Это:
1. Генерирует 100 шаблонов
2. Тестирует поиск
3. Запускает API

## 📡 API Endpoints

### Информация о системе
```bash
GET /api/info
```

**Ответ:**
```json
{
  "status": "ok",
  "templates_available": 1000,
  "layout_types": ["modern_minimal", "corporate_bold", ...],
  "message": "1000 шаблонов готово к использованию"
}
```

### Поиск по промту
```bash
POST /api/search
```

**Запрос:**
```json
{
  "prompt": "Нужен современный синий дизайн минималистичный",
  "limit": 5
}
```

**Ответ:**
```json
{
  "success": true,
  "query": "Нужен современный синий дизайн минималистичный",
  "count": 5,
  "templates": [
    {
      "id": "template_00001",
      "filename": "template_00001.pdf",
      "layout_type": "modern_minimal",
      "description": "Современный минималистичный дизайн...",
      "relevance_score": 0.85,
      "tags": ["modern_minimal", "синий", "минималистичный", ...],
      "pages": 12
    },
    ...
  ]
}
```

### Поиск по тегам
```bash
POST /api/search-by-tags
```

**Запрос:**
```json
{
  "tags": ["синий", "минималистичный"],
  "limit": 10
}
```

### Получить по типу макета
```bash
GET /api/layout/modern_minimal?limit=10
```

### Случайные шаблоны
```bash
GET /api/random?count=5
```

### Скачать шаблон
```bash
GET /api/download/template_00001
```

Скачает файл `template_00001.pdf`

### Получить метаданные
```bash
GET /api/metadata/template_00001
```

### Список типов макетов
```bash
GET /api/layout-types
```

### Проверка здоровья
```bash
GET /api/health
```

## 🔧 Параметры генерирования

### Типы макетов (10 вариантов)
- `modern_minimal` - Современный минималистичный
- `corporate_bold` - Корпоративный смелый
- `creative_artistic` - Творческий
- `tech_focused` - Технологичный
- `nature_inspired` - Вдохновлённый природой
- `minimalist_clean` - Минималистичный чистый
- `dark_professional` - Тёмный профессиональный
- `colorful_vibrant` - Яркий многоцветный
- `elegant_classic` - Элегантный классический
- `startup_modern` - Стартап современный

### Цветовые схемы (10 вариантов)
Каждый шаблон имеет уникальную комбинацию основного, вторичного и акцентного цветов.

### Теги (автоматические)
Каждому шаблону присваиваются теги по категориям:
- **Тип макета**: modern_minimal, corporate_bold, etc.
- **Цвета**: синий, зелёный, красный, и т.д.
- **Стиль**: современный, минималистичный, творческий, и т.д.
- **Назначение**: портфолио, презентация, каталог, и т.д.

## 📊 Статистика шаблонов

При генерировании 1000 шаблонов:
- **Всего PDF файлов:** 1000
- **Общий размер:** ~500-700 МБ
- **Среднее количество страниц:** 12
- **Всего мегабайт данных:** 500-700
- **JSON метаданные:** ~50-100 МБ

## 🔗 Интеграция с основным проектом

Добавьте в `backend/app.py`:

```python
from portfolio_templates_generator.search_engine import TemplateSearchEngine

templates_engine = TemplateSearchEngine(
    metadata_dir='../portfolio_templates_generator/pdf_templates/metadata',
    index_file='../portfolio_templates_generator/pdf_templates/index.json'
)

@app.route('/api/templates/search', methods=['POST'])
def search_templates_endpoint():
    data = request.json
    prompt = data.get('prompt', '')
    results = templates_engine.search(prompt, limit=5)
    return jsonify({'success': True, 'templates': results})
```

## 💾 Кэширование и оптимизация

- Индекс загружается один раз при запуске
- TF-IDF матрица кэшируется в памяти
- JSON метаданные не пересчитываются
- Поиск работает за O(1) на количество шаблонов

## 🐛 Решение проблем

### Ошибка: "Шаблоны не найдены"
Запустите генерирование:
```bash
python main.py --generate 1000
```

### API не запускается на порту 5001
Измените порт в `api.py`:
```python
app.run(port=5002)  # Используем другой порт
```

### Слишком долго генерируется
Уменьшите количество шаблонов:
```bash
python main.py --generate 100  # Вместо 1000
```

## 📝 Лицензия

MIT License

---

**Версия:** 1.0.0  
**Статус:** ✅ Готово к использованию
