# 🚀 Быстрый старт AI Portfolio Generator v2.0

## ⚡ За 5 минут до первого дизайна

### Вариант 1: Автоматическое генерирование (РЕКОМЕНДУЕТСЯ)

**Шаг 1:** Двойной клик на `generate_designs.bat`

```
Что происходит:
1. ✓ Проверяет Python
2. ✓ Устанавливает зависимости
3. ✓ Скачивает портфолио
4. ✓ Обучает нейросеть
5. ✓ Генерирует 10 дизайнов
6. ✓ Создаёт HTML и PDF файлы
7. ✓ Создаёт галерею для просмотра
```

**Шаг 2:** Откройте `backend/generated_portfolios/gallery.html` в браузере

✨ Вот и всё! Вы видите 10 AI-сгенерированных дизайнов

---

### Вариант 2: Запуск API сервера

**Шаг 1:** Двойной клик на `start_api.bat`

```
Сервер запустится на http://localhost:5000
```

**Шаг 2:** Откройте в браузере:
```
file:///c:/Users/ilaci/portfolio%20constructor/portfolio-constructor/frontend/index_v2.html
```

**Шаг 3:** Используйте интерфейс:

- **🤖 AI Mix Designer** - генерируйте дизайны прямо в браузере
- **🔍 Search Portfolios** - ищите реальные портфолио FTC
- **✨ Generate Layouts** - создавайте макеты по описанию
- **⚙️ Admin** - управляйте портфолио

---

## 🎨 Что вы получаете?

Каждый сгенерированный дизайн включает:

✅ **HTML портфолио** - готовый для использования в браузере
✅ **PDF файл** - можно скачивать и распечатывать
✅ **Цветовая схема** - 3 гармоничных цвета
✅ **Типография** - заголовки и текст
✅ **Дизайн паттерн** - один из 6 стилей
✅ **Уверенность AI** - процент уверенности нейросети

---

## 📊 Как это работает?

```
Ваше портфолио может выглядеть так:

Успешное портфолио #1
├─ Цвета: синий + фиолетовый
├─ Шрифты: Inter + Open Sans
├─ Стиль: modern
└─ Уверенность: 94%

Успешное портфолио #2
├─ Цвета: розовый + жёлтый
├─ Шрифты: Poppins + Lato
├─ Стиль: gradient
└─ Уверенность: 87%

┌─────────────────────┐
│   НЕЙРОСЕТЬ AI      │ ← анализирует оба
│  (обучена на них)   │
└─────────────────────┘

┌──────────────────────────────┐
│ НОВЫЙ ДИЗАЙН (микс обоих)    │
├─ Цвета: фиолетовый + розовый │
├─ Шрифты: Raleway + Roboto    │
├─ Стиль: glassmorphism        │
└─ Уверенность: 82% ✨         │
└──────────────────────────────┘
```

---

## 🎯 Примеры использования

### Пример 1: Генерировать 15 дизайнов

**Командная строка:**
```bash
cd backend
python ai_portfolio_generator_v2.py
```

В коде поменяйте:
```python
generator.run_full_pipeline(generate_count=15)  # Вместо 10
```

### Пример 2: Использовать только API

```bash
python app_v2.py
```

Потом через REST:
```bash
curl -X POST http://localhost:5000/api/generate-ai-mix \
  -H "Content-Type: application/json" \
  -d '{"count": 5}'
```

### Пример 3: Загрузить свои портфолио

```python
import json
from portfolio_design_neural import PortfolioMixGenerator

# Загрузить свои данные
with open('my_portfolios.json') as f:
    portfolios = json.load(f)

# Создать генератор
generator = PortfolioMixGenerator(portfolios)

# Генерировать
designs = generator.generate_mix_designs(count=10)

# Использовать дизайны...
```

---

## 🐛 Если что-то не работает

### Ошибка: "Python не найден"
**Решение:** Установите Python 3.8+ с https://python.org
- Убедитесь что отмечена галочка "Add Python to PATH"

### Ошибка: "Selenium not found"
**Решение:** Вручную установите зависимости
```bash
pip install selenium beautifulsoup4 reportlab scikit-learn numpy
```

### Ошибка: "Chrome WebDriver not found"
**Решение:** Скрэйпер автоматически использует fallback (BeautifulSoup)
- Приложение будет работать, но без полного скрэйпинга

### Сервер не запускается на localhost:5000
**Решение:** Порт может быть занят
```bash
# Windows - освободить порт
netstat -ano | findstr :5000
taskkill /PID [PID] /F
```

---

## 💡 Советы

### 1. Быстрое генерирование
```bash
python ai_portfolio_generator_v2.py
# Скорость: ~30 секунд на 10 дизайнов
```

### 2. Экспериментируйте с параметрами
Отредактируйте в скрипте:
```python
generator.neural_net.num_hidden = 32  # Больше нейронов = точнее
generator.neural_net.train(epochs=200)  # Больше эпох = лучше
```

### 3. Используйте галерею
```
backend/generated_portfolios/gallery.html
```
Красивый предпросмотр всех дизайнов с:
- Прямой просмотр HTML
- Скачивание PDF
- Цветовые схемы
- Информация о дизайне

### 4. Интегрируйте в свой проект
```python
from portfolio_design_neural import PortfolioMixGenerator
from portfolio_visualizer import PortfolioDesignVisualizer

generator = PortfolioMixGenerator(portfolios)
designs = generator.generate_mix_designs(count=5)

visualizer = PortfolioDesignVisualizer()
for design in designs:
    html = visualizer.generate_html_portfolio(design)
    pdf = visualizer.generate_pdf_portfolio(design)
    # Использовать как вам нужно...
```

---

## 📁 Структура папок

```
backend/
├── generated_portfolios/      ← ВСЕ ДИЗАЙНЫ ЗДЕСЬ
│   ├── generated_1.html
│   ├── generated_1.pdf
│   ├── generated_2.html
│   ├── generated_2.pdf
│   ├── ...
│   ├── gallery.html           ← ОТКРОЙТЕ ЭТОТ ФАЙЛ
│   └── designs_index.json
│
├── data/
│   ├── ftc_portfolios.json    ← База портфолио
│   └── portfolio_templates.json
│
├── downloaded_portfolios/     ← Скачанные PDF (если запускали шаг 2)
│   └── Team_10179_cs.pdf
│
└── [остальные файлы]
```

---

## 🎓 Что дальше?

### Базовое использование (уже делает)
- ✅ Генерировать дизайны
- ✅ Скачивать как HTML/PDF
- ✅ Искать реальные портфолио
- ✅ Просматривать в галерее

### Продвинутое (можно добавить)
- 📍 Редактор дизайна в реальном времени
- 📍 Сохранение истории генераций
- 📍 Импорт собственных портфолио
- 📍 GAN для совершенно новых дизайнов
- 📍 Анализ изображений (CNN)

---

## 🔗 Полезные ссылки

- 📖 [Полная документация](README_AI_v2.md)
- 🌐 [Сайт FTC портфолио](https://portfolios.hivemindrobotics.net/ftc)
- 🐍 [Python 3.8+](https://python.org)
- 🔧 [Flask документация](https://flask.palletsprojects.com)

---

## 📞 Помощь

Если не работает:
1. Проверьте что Python установлен: `python --version`
2. Переустановите зависимости: `pip install -r requirements.txt`
3. Запустите скрипт с параметром debug:
   ```bash
   python -u ai_portfolio_generator_v2.py
   ```

---

**Готово! Начните генерировать дизайны! 🚀✨**

**Просто запустите:** `generate_designs.bat` или `start_api.bat`
