# 🚀 Быстрый старт FTC Portfolio Constructor

## Что это?
Приложение для **генерирования портфолио команд FTC** из готовых шаблонов и **сбора данных с сайта Hivemind Robotics**.

## Что уже готово?
✅ Фронтенд интерфейс (веб-приложение)
✅ Бэкенд с Flask API
✅ База готовых шаблонов портфолио
✅ Поддержка работы с данными FTC
✅ **Псевдогенерирование** - рандомный выбор из готовых шаблонов

## Быстрый запуск

### Способ 1: Python скрипт (рекомендуется)
```bash
cd "c:\Users\ilaci\portfolio constructor\portfolio-constructor"
python start_app.py
```

### Способ 2: BAT файл (Windows)
Просто **дважды кликните** на файл:
```
run_app.bat
```

### Способ 3: Вручную
```bash
cd "c:\Users\ilaci\portfolio constructor\portfolio-constructor\backend"
python -m pip install -r requirements.txt
python app.py
```

## Что происходит после запуска?
1. Установляются зависимости (первый запуск может занять 1-2 минуты)
2. Запускается веб-сервер на `http://localhost:5000`
3. Откройте браузер и перейдите по этому адресу

## Как пользоваться?

### Вкладка "Генератор"
1. Введите описание портфолио:
   - Пример: "Нужен современный дизайн с синей палитрой"
   - Пример: "Минималистичный портфолио для робо-клуба"
   
2. Нажмите "Сгенерировать макеты"

3. Система выберет 3 подходящих шаблона из готовых

4. Кликните на понравившийся макет для просмотра

### Вкладка "Администратор"
Здесь можно:
- Сканировать сайт `https://portfolios.hivemindrobotics.net/ftc` для обновления данных
- Загружать новые портфолио команд

### Вкладка "Поиск портфолио"
- Поиск по названию команды
- Поиск по номеру команды
- Просмотр загруженных портфолио

## Основные файлы

| Файл | Описание |
|------|----------|
| `start_app.py` | Основной скрипт запуска (Python) |
| `run_app.bat` | Запуск для Windows (BAT) |
| `backend/app.py` | Flask приложение (API) |
| `backend/portfolio_generator.py` | Логика генерирования |
| `frontend/index.html` | Веб-интерфейс |
| `frontend/script.js` | JavaScript логика |
| `backend/data/portfolio_templates.json` | Готовые шаблоны |

## Текущая функциональность

### ✅ Работает:
- Поиск и фильтрация портфолио
- Псевдогенерирование из готовых шаблонов
- Просмотр макетов в браузере
- Выбор цветовой схемы
- Скачивание HTML макетов

### 🔧 Можно добавить:
- Настройку шаблонов через интерфейс
- Реальное ИИ генерирование (OpenAI API)
- Экспорт в PDF
- Сохранение избранных макетов
- Версионирование портфолио

## Если что-то не работает

### Проблема: "Не найдены зависимости"
```bash
python -m pip install Flask Flask-CORS scikit-learn requests beautifulsoup4
```

### Проблема: "Порт 5000 уже занят"
Измените в `app.py` последнюю строку:
```python
app.run(debug=True, host='0.0.0.0', port=5001)  # Используем 5001
```
Затем откройте `http://localhost:5001`

### Проблема: "Шаблоны не загружаются"
Убедитесь что файл `backend/data/portfolio_templates.json` существует и не повреждён.

## Структура проекта
```
portfolio-constructor/
├── start_app.py          ← Запуск приложения
├── run_app.bat           ← BAT для Windows
├── backend/
│   ├── app.py            ← Flask сервер
│   ├── portfolio_generator.py
│   ├── portfolio_visualizer.py
│   ├── requirements.txt   ← Зависимости
│   └── data/
│       └── portfolio_templates.json
└── frontend/
    ├── index.html        ← Главная страница
    ├── script.js         ← JavaScript
    └── styles.css        ← Стили
```

## Следующие шаги

1. **Попробуйте генератор** - введите описание и посмотрите результаты
2. **Проверьте поиск** - найдите существующие портфолио
3. **Сканируйте сайт** - загрузите данные с Hivemind Robotics (вкладка Администратор)

---

**Автор:** AI Portfolio Generator v2  
**Статус:** ✅ Готово к использованию  
**Версия:** 1.0.0
