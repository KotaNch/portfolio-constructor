# 🚀 Быстрый старт

## Вариант 1: Автоматический запуск (Windows)

Просто запустите файл `start.bat` в корневой папке проекта.

## Вариант 2: Ручной запуск

### 1. Откройте терминал/PowerShell в папке проекта

```powershell
cd "c:\Users\ilaci\portfolio constructor\portfolio-constructor"
```

### 2. Установите зависимости (если не установлены)

```bash
cd backend
pip install -r requirements.txt
```

### 3. В одном терминале запустите Flask сервер

```bash
cd backend
python app.py
```

Вы должны увидеть:
```
 * Running on http://0.0.0.0:5000
 * Debug mode: on
```

### 4. В другом терминале/браузере откройте фронтенд

**Вариант A - Прямое открытие:**
```
Откройте в браузере: file:///c:/Users/ilaci/portfolio%20constructor/portfolio-constructor/frontend/index.html
```

**Вариант B - С помощью HTTP сервера:**
```bash
cd frontend
python -m http.server 8000
```
Затем откройте: `http://localhost:8000`

## ✅ Проверка работы

1. Откройте приложение в браузере
2. Вы должны увидеть красивый интерфейс с тремя вкладками
3. Перейдите на вкладку "Администратор"
4. Нажмите "Сканировать сайт и обновить данные"
5. Дождитесь загрузки портфолио

## 🐛 Если что-то не работает

### Ошибка: "Backend not available"
- Убедитесь, что Flask сервер запущен (терминал должен показывать "Running on...")
- Проверьте, что используется `http://localhost:5000`, а не другой порт
- Если нужен другой порт, измените в [backend/app.py](backend/app.py) и [frontend/script.js](frontend/script.js)

### ModuleNotFoundError
- Убедитесь, что зависимости установлены: `pip install -r requirements.txt`
- Проверьте версию Python: `python --version` (нужна 3.8+)

### Портфолио не загружаются
- Нажмите "Сканировать сайт" на вкладке "Администратор"
- Дождитесь сообщения об успехе
- Перезагрузите страницу браузера

## 📝 API тестирование

Можете протестировать API напрямую:

```bash
# Проверка здоровья сервера
curl http://localhost:5000/api/health

# Поиск портфолио
curl "http://localhost:5000/api/search-ftc?q=tech&limit=10"

# Генерация макетов
curl -X POST http://localhost:5000/api/generate-portfolio \
  -H "Content-Type: application/json" \
  -d "{\"prompt\": \"modern design\"}"
```

## 🎯 Следующие шаги

1. **Поиск портфолио** - перейдите на вкладку "Поиск портфолио"
2. **Скачивание** - выберите портфолио и нажмите "Скачать"
3. **Генерация** - перейдите на вкладку "Генератор" и опишите требования
4. **Администрирование** - обновляйте базу данных по необходимости

Для подробной информации смотрите [README.md](README.md)
