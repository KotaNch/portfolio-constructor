# 🎉 FTC Portfolio Constructor - Система готова!

Полная система для создания и генерирования портфолио для FTC команд.

## 📁 Структура проекта

```
portfolio-constructor/
│
├── 🚀 ГЛАВНОЕ ПРИЛОЖЕНИЕ
│   ├── backend/                          (Flask API)
│   │   ├── app.py                        (главное приложение)
│   │   ├── portfolio_generator.py        (логика генерирования)
│   │   ├── portfolio_visualizer.py       (визуализация)
│   │   ├── scraper.py                    (сбор данных FTC)
│   │   ├── requirements.txt              (зависимости)
│   │   └── data/
│   │       ├── portfolio_templates.json
│   │       └── ftc_portfolios.json
│   │
│   ├── frontend/                         (Веб интерфейс)
│   │   ├── index.html                    (главная страница)
│   │   ├── script.js                     (логика)
│   │   └── styles.css                    (стили)
│   │
│   ├── run_app.bat                       (запуск для Windows)
│   ├── start_app.py                      (запуск для Python)
│   └── START_HERE_SIMPLE.md              (инструкция)
│
└── 📚 ГЕНЕРАТОР ШАБЛОНОВ (отдельный проект)
    └── portfolio_templates_generator/
        ├── main.py                       (главный скрипт)
        ├── generate_templates.py         (генератор PDF)
        ├── search_engine.py             (поиск TF-IDF)
        ├── api.py                       (REST API)
        ├── config.py                    (конфигурация)
        ├── requirements.txt             (зависимости)
        ├── quick_start.py               (быстрый старт)
        ├── START.bat                    (запуск Windows)
        ├── README.md                    (документация)
        ├── USAGE.md                     (руководство)
        ├── ARCHITECTURE.md              (архитектура)
        └── pdf_templates/               (выходная папка)
            ├── template_00001.pdf       (1000+)
            ├── metadata/
            └── index.json
```

## 🚀 Быстрый старт (2 варианта)

### Вариант 1: Основное приложение

```bash
cd backend
python -m pip install -r requirements.txt
python app.py
```

Откройте браузер: http://localhost:5000

### Вариант 2: Генератор шаблонов

```bash
cd portfolio_templates_generator
python quick_start.py
```

Откройте браузер: http://localhost:5001

## 💡 Использование

### Основное приложение (Генератор + Поиск)

1. **Генератор**: Введите описание портфолио → получите 3 подходящих макета
2. **Администратор**: Сканируйте сайт FTC → загружайте новые портфолио

### Генератор шаблонов (1000+ готовых PDF)

1. **Поиск**: POST `/api/search` с промтом → получите 5 подходящих PDF
2. **Скачивание**: GET `/api/download/<id>` → скачайте выбранный PDF
3. **Метаданные**: GET `/api/metadata/<id>` → информация о шаблоне

## 📊 Возможности

### Основное приложение
✅ Генерирование макетов из готовых шаблонов  
✅ Поиск портфолио по FTC командам  
✅ Сканирование данных с сайта Hivemind Robotics  
✅ Просмотр в браузере  

### Генератор шаблонов
✅ Генерирование 1000+ готовых PDF  
✅ Автоматическая каталогизация  
✅ Умный поиск по TF-IDF  
✅ REST API для интеграции  
✅ Скачивание готовых файлов  

## 📡 API

### Основное приложение (порт 5000)

| Метод | Endpoint | Описание |
|-------|----------|---------|
| POST | `/api/generate-portfolio` | Генерирование макетов |
| GET | `/api/search-ftc` | Поиск FTC портфолио |
| POST | `/api/scan-portfolios` | Сканирование сайта |

### Генератор шаблонов (порт 5001)

| Метод | Endpoint | Описание |
|-------|----------|---------|
| POST | `/api/search` | Поиск по промту |
| GET | `/api/random` | Случайные шаблоны |
| GET | `/api/download/<id>` | Скачать PDF |
| GET | `/api/metadata/<id>` | Метаданные |

## 🎯 Основные фичи

### Для пользователей
- 🎨 Выбор из 1000+ готовых дизайнов
- 🔍 Умный поиск по описанию
- 📥 Скачивание PDF портфолио
- 🏆 Просмотр достижений FTC команд

### Для администраторов
- ⚙️ Управление шаблонами
- 📊 Сканирование данных FTC
- 🏷️ Автоматическое тегирование
- 📈 Анализ популярности

## 🔧 Интеграция

Два сервиса работают независимо но могут быть интегрированы:

```python
# backend/app.py
from portfolio_templates_generator.search_engine import TemplateSearchEngine

templates_engine = TemplateSearchEngine()

@app.route('/api/combined-search', methods=['POST'])
def combined_search():
    prompt = request.json.get('prompt')
    
    # Получить из основной системы
    designs = generator.generate_layouts(prompt, num_examples=3)
    
    # Получить готовые PDF
    templates = templates_engine.search(prompt, limit=5)
    
    return jsonify({
        'designs': designs,
        'pdf_templates': templates
    })
```

## 📖 Документация

### Основное приложение
- [START_HERE_SIMPLE.md](START_HERE_SIMPLE.md) - Быстрый старт
- [backend/README.md](backend/README.md) - Техническая документация

### Генератор шаблонов
- [portfolio_templates_generator/README.md](portfolio_templates_generator/README.md) - Техническая
- [portfolio_templates_generator/USAGE.md](portfolio_templates_generator/USAGE.md) - Руководство пользователя
- [portfolio_templates_generator/ARCHITECTURE.md](portfolio_templates_generator/ARCHITECTURE.md) - Архитектура

## ⏱️ Производительность

| Операция | Время |
|----------|-------|
| Запуск основного приложения | < 5 сек |
| Запуск генератора | < 1 сек |
| Генерирование 100 PDF | 2-3 мин |
| Поиск по промту | < 100 мс |
| Скачивание PDF | зависит от сети |

## 🛠️ Требования

### Основное приложение
- Python 3.7+
- Flask, scikit-learn, requests, beautifulsoup4
- reportlab (для PDF)

### Генератор шаблонов
- Python 3.7+
- reportlab, scikit-learn, Flask, numpy

Установка всех зависимостей:
```bash
# Основное приложение
cd backend
pip install -r requirements.txt

# Генератор
cd portfolio_templates_generator
pip install -r requirements.txt
```

## 🚀 Развёртывание

### Локально
```bash
# Terminal 1: Основное приложение
cd backend && python app.py

# Terminal 2: Генератор (опционально)
cd portfolio_templates_generator && python api.py
```

### Docker (рекомендуется для production)
```dockerfile
# Dockerfile для основного приложения
FROM python:3.9
WORKDIR /app
COPY backend/ .
RUN pip install -r requirements.txt
EXPOSE 5000
CMD ["python", "app.py"]
```

## 📝 Примеры использования

### Python
```python
# Генератор макетов
from backend.portfolio_generator import PortfolioGenerator
gen = PortfolioGenerator()
designs = gen.generate_layouts("синий минималистичный", num_examples=3)

# Поиск PDF шаблонов
from portfolio_templates_generator.search_engine import TemplateSearchEngine
engine = TemplateSearchEngine()
templates = engine.search("современный дизайн", limit=5)
```

### JavaScript
```javascript
// Получить макеты
const designs = await fetch('/api/generate-portfolio', {
  method: 'POST',
  body: JSON.stringify({prompt: 'синий минимализм'})
}).then(r => r.json());

// Получить PDF шаблоны
const templates = await fetch('/api/templates/search', {
  method: 'POST',
  body: JSON.stringify({prompt: 'синий минимализм'})
}).then(r => r.json());
```

## 🐛 Решение проблем

### Основное приложение не запускается
```bash
# Проверить что Flask установлен
python -m pip list | grep Flask

# Переустановить зависимости
pip install -r backend/requirements.txt
```

### Генератор не найден
```bash
# Проверить что папка существует
ls portfolio_templates_generator/

# Установить зависимости
pip install -r portfolio_templates_generator/requirements.txt
```

### Порты уже занять
```bash
# Изменить порт в app.py
app.run(port=5002)
```

## 📞 Поддержка

- Основное приложение: http://localhost:5000
- Генератор: http://localhost:5001
- API Документация: смотри README в каждой папке

## 📄 Лицензия

MIT License

---

**Версия:** 2.0.0  
**Статус:** ✅ Production Ready  
**Дата:** 14 января 2026
